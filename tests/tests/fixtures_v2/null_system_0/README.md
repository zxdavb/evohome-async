# fixtures_v2/null_system_0

Synthesised fixture — null installation: an account with no locations.

## System

- 0 locations in `user_locations.json`

The `null_system_*` fixtures each stop the location → gateway → TCS hierarchy at a
different level; this one stops at the account.

## Files

| File | Source |
|------|--------|
| `user_account.json` | Synthesised — userId 4000001, UK |
| `user_locations.json` | Synthesised — empty array |

## Notes

- Formerly `system_008/`.
